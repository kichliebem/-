# 水力-热力耦合最小系统（Obsidian 兼容 Markdown + LaTeX）

> 拓扑（在原先水力网络基础上，仅在三通后的两条支路各加入一个与环境换热的换热器，忽略其他部位的热交换；源头来水温度为 16℃，环境温度 30℃）：
>
> ```
> 水源(16℃) → 管道 → 阀门 → 三通
>                                ├─ 换热器 HX₁ → 罐体1
>                                └─ 换热器 HX₂ → 罐体2
> 环境温度：30℃
> ```
>
> 仅考虑换热器内与环境的热量交换；水力部分仍按先前拓扑与方程构建。

---

## 变量与方程编号

- 节点压力向量（6 个节点）：
  $$
  p=\begin{bmatrix}p_1 & p_2 & p_3 & p_4 & p_5 & p_6\end{bmatrix}^T
  $$

- 支路质量流量向量（5 条支路：$b_1,\dots,b_5$）：
  $$
  \dot{m}=\begin{bmatrix}\dot{m}_1 & \dot{m}_2 & \dot{m}_3 & \dot{m}_4 & \dot{m}_5\end{bmatrix}^T
  $$

- 节点温度向量（与节点一一对应，$T_1$ 为源头水源温度，给定为 16℃）：
  $$
  T=\begin{bmatrix}T_1 & T_2 & T_3 & T_4 & T_5 & T_6\end{bmatrix}^T
  $$

- 残差向量由三类方程组成（并按此顺序堆叠）：
  1) 质量守恒（节点流量平衡）$R^{(m)}\in\mathbb{R}^{6}$  
  2) 压降/设备方程（支路方程）$R^{(p)}\in\mathbb{R}^{5}$  
  3) 热力方程 $R^{(h)}\in\mathbb{R}^{6}$：  
     - 3 个“无热损”约束（源→管→阀→三通沿线等温传递）：$T_2=T_1,\ T_3=T_2,\ T_4=T_3$  
     - 2 个换热器能量方程（$HX_1$：$4\to5$；$HX_2$：$4\to6$）  
     - 1 个源头温度约束：$T_1=16\ ^\circ\mathrm{C}$

> 变量总数：$6+5+6=17$  
> 方程总数：$6+5+6=17$

---

## 水力关联矩阵与阻抗矩阵（与纯水力相同）

- 关联矩阵 $A\in\mathbb{R}^{6\times5}$（行=节点，列=支路；流入为 $+1$，流出为 $-1$）：

$$
A=
\begin{bmatrix}
-1 & 0 & 0 & 0 & 0 \\[4pt]
 1 & -1 & 0 & 0 & 0 \\[4pt]
 0 &  1 & -1 & 0 & 0 \\[4pt]
 0 &  0 &  1 & -1 & -1 \\[4pt]
 0 &  0 &  0 &  1 &  0 \\[4pt]
 0 &  0 &  0 &  0 &  1
\end{bmatrix}
$$

- 阻抗矩阵 $D=\mathrm{diag}(D_1,\dots,D_5)$，各支路压降关于流量的一阶导数（示例形式）：

$$
D_b=\frac{\partial\Delta p_b}{\partial \dot{m}_b},\qquad
\begin{cases}
\text{管道：} & \displaystyle \frac{\partial\Delta p}{\partial \dot{m}}=2\,k\,\dot{m},\quad
k=f\,\dfrac{L}{D\,A^2\,2\rho} \\[10pt]
\text{阀门：} & \displaystyle \frac{\partial\Delta p}{\partial \dot{m}}=\frac{2\,\dot{m}}{C_v^2}
\end{cases}
$$

> 本耦合场景下，$D$ 不含显式温度项（忽略物性随温度变动的影响）；如需考虑，可在 $D(T)$ 中引入 $\mu(T),\ \rho(T)$ 等依赖。

---

## 热力子系统建模（仅换热器与环境换热）

- 环境温度（常量）：$T_{\mathrm{amb}}=30\ ^\circ\mathrm{C}$  
- 定压比热：$c_p$（常数，单位 $\mathrm{J/(kg\cdot K)}$）  
- 两台换热器（$HX_1$：支路 $b_4:4\to5$；$HX_2$：支路 $b_5:4\to6$），采用对环境的稳态能量平衡（简化为“对流到环境”的 $UA$ 模型）：
  - $HX_1$ 能量方程：
    $$
    R^{(h)}_{HX_1} \equiv \dot{m}_4\,c_p\,(T_4-T_5)\;-\;UA_1\,\Big(\tfrac{T_4+T_5}{2}-T_{\mathrm{amb}}\Big)=0
    $$
  - $HX_2$ 能量方程：
    $$
    R^{(h)}_{HX_2} \equiv \dot{m}_5\,c_p\,(T_4-T_6)\;-\;UA_2\,\Big(\tfrac{T_4+T_6}{2}-T_{\mathrm{amb}}\Big)=0
    $$

- “无热损”等温传递约束（源→管→阀→三通）：
  $$
  R^{(h)}_{12}\equiv T_2-T_1=0,\qquad
  R^{(h)}_{23}\equiv T_3-T_2=0,\qquad
  R^{(h)}_{34}\equiv T_4-T_3=0
  $$

- 源头温度约束：
  $$
  R^{(h)}_{\mathrm{src}}\equiv T_1-16=0
  $$

> 若需采用更精细的换热模型，可用 $\varepsilon$–NTU 或 LMTD：  
> $Q=\varepsilon\,\dot{m}\,c_p\,(T_{\mathrm{in}}-T_{\mathrm{amb}})$ 或 $Q=UA\,\Delta T_{\mathrm{lm}}$，并据此对 $R^{(h)}$ 与雅可比进行替换。

---

## 总体牛顿迭代的雅可比分块结构

- 变量堆叠：
  $$
  \Delta x=\begin{bmatrix}\Delta p \\[2pt] \Delta\dot{m} \\[2pt] \Delta T\end{bmatrix}
  ,\qquad
  R=\begin{bmatrix}R^{(m)} \\[2pt] R^{(p)} \\[2pt] R^{(h)}\end{bmatrix}
  $$

- 牛顿步：
  $$
  J\,\Delta x=-R
  $$

- 分块雅可比（忽略水力对温度的直接一阶耦合，即物性不随温度变）：
  $$
  J=
  \begin{bmatrix}
  \dfrac{\partial R^{(m)}}{\partial p} & \dfrac{\partial R^{(m)}}{\partial \dot{m}} & \dfrac{\partial R^{(m)}}{\partial T} \\[8pt]
  \dfrac{\partial R^{(p)}}{\partial p} & \dfrac{\partial R^{(p)}}{\partial \dot{m}} & \dfrac{\partial R^{(p)}}{\partial T} \\[8pt]
  \dfrac{\partial R^{(h)}}{\partial p} & \dfrac{\partial R^{(h)}}{\partial \dot{m}} & \dfrac{\partial R^{(h)}}{\partial T}
  \end{bmatrix}
  =
  \begin{bmatrix}
  0 & A & 0 \\[4pt]
  A^T & -D & 0 \\[4pt]
  0 & H_{\dot{m}} & H_T
  \end{bmatrix}
  $$

### 热力雅可比子块的明确表达

- $H_{\dot{m}}\in\mathbb{R}^{6\times5}$：仅两行（对应 $HX_1, HX_2$）对 $\dot{m}_4,\dot{m}_5$ 非零，其余行为 $0$：
  $$
  \begin{aligned}
  \frac{\partial R^{(h)}_{HX_1}}{\partial \dot{m}_4}&=c_p\,(T_4-T_5), &
  \frac{\partial R^{(h)}_{HX_1}}{\partial \dot{m}_j}&=0\ (j\neq4), \\[6pt]
  \frac{\partial R^{(h)}_{HX_2}}{\partial \dot{m}_5}&=c_p\,(T_4-T_6), &
  \frac{\partial R^{(h)}_{HX_2}}{\partial \dot{m}_j}&=0\ (j\neq5).
  \end{aligned}
  $$

- $H_T\in\mathbb{R}^{6\times6}$：由 3 条等温约束 + 2 条换热器能量方程 + 1 条源温约束组成（按上述 $R^{(h)}$ 行顺序堆叠）。  
  1) 等温约束三行（$T_2\!-\!T_1,\ T_3\!-\!T_2,\ T_4\!-\!T_3$）：
     $$
     \begin{aligned}
     \frac{\partial (T_2-T_1)}{\partial T} &=
     \begin{bmatrix}-1 & 1 & 0 & 0 & 0 & 0\end{bmatrix} \\[4pt]
     \frac{\partial (T_3-T_2)}{\partial T} &=
     \begin{bmatrix}0 & -1 & 1 & 0 & 0 & 0\end{bmatrix} \\[4pt]
     \frac{\partial (T_4-T_3)}{\partial T} &=
     \begin{bmatrix}0 & 0 & -1 & 1 & 0 & 0\end{bmatrix}
     \end{aligned}
     $$
  2) 换热器两行（$HX_1,\ HX_2$）：
     $$
     \begin{aligned}
     \frac{\partial R^{(h)}_{HX_1}}{\partial T} &=
     \begin{bmatrix}
     0 & 0 & 0 &\ \ \ \dot{m}_4\,c_p-\dfrac{UA_1}{2}\ \ & -\dot{m}_4\,c_p-\dfrac{UA_1}{2} & 0
     \end{bmatrix} \\[8pt]
     \frac{\partial R^{(h)}_{HX_2}}{\partial T} &=
     \begin{bmatrix}
     0 & 0 & 0 &\ \ \ \dot{m}_5\,c_p-\dfrac{UA_2}{2}\ \ & 0 & -\dot{m}_5\,c_p-\dfrac{UA_2}{2}
     \end{bmatrix}
     \end{aligned}
     $$
  3) 源温约束一行（$T_1-16=0$）：
     $$
     \frac{\partial (T_1-16)}{\partial T}=\begin{bmatrix}1 & 0 & 0 & 0 & 0 & 0\end{bmatrix}
     $$

> 若选用 $Q=UA\,(T_{\mathrm{out}}-T_{\mathrm{amb}})$ 或 LMTD 模型，只需将上述 $R^{(h)}_{HX_i}$ 与其偏导替换为相应表达式即可。

---

## 完整的雅可比尺寸与结构

- 变量顺序：$\big[p_1,\dots,p_6\;\big|\;\dot{m}_1,\dots,\dot{m}_5\;\big|\;T_1,\dots,T_6\big]^T$
- 方程顺序：$\big[R^{(m)}\ (6)\ ;\ R^{(p)}\ (5)\ ;\ R^{(h)}\ (6)\big]^T$

块结构（尺寸标注）：

$$
J=
\begin{bmatrix}
\color{gray}{0_{6\times6}} & \color{gray}{A_{6\times5}} & \color{gray}{0_{6\times6}} \\[6pt]
\color{gray}{A^T_{5\times6}} & \color{gray}{(-D)_{5\times5}} & \color{gray}{0_{5\times6}} \\[6pt]
\color{gray}{0_{6\times6}} & \color{gray}{H_{\dot{m}}{}_{6\times5}} & \color{gray}{H_T{}_{6\times6}}
\end{bmatrix}
$$

右端项（残差堆叠）：

$$
R=
\begin{bmatrix}
R^{(m)} \\[2pt]
R^{(p)} \\[2pt]
R^{(h)}
\end{bmatrix}
,\qquad
\text{牛顿步：}\quad
J\,\Delta x=-R
$$

---

## 换热器能量方程的物理意义与数值提示

- $R^{(h)}_{HX_i}=0$ 表示“流体带走/带入的焓变”与“对环境的热交换”在稳态下相等：
  $$
  \dot{m}_i\,c_p\,(T_{\mathrm{in}}-T_{\mathrm{out}})=UA_i\left(\frac{T_{\mathrm{in}}+T_{\mathrm{out}}}{2}-T_{\mathrm{amb}}\right)
  $$
- 数值求解建议：
  1) 初始温度可用 $T_1=16^\circ\mathrm{C}$，$T_2=T_3=T_4=16^\circ\mathrm{C}$，$T_5,T_6$ 可先取 $(T_4+T_{\mathrm{amb}})/2$。  
  2) 初始流量来自纯水力解；先解 $\{p,\dot{m}\}$，再启用热力方程耦合求解可提升收敛。  
  3) $UA_i$ 较大或流量很小的情况下，$H_T$ 可能导致刚性增强；必要时对热力方程做松弛或使用线搜索。

---

## 总结

1) 在原水力块
$$
\begin{bmatrix}0 & A\\ A^T & -D\end{bmatrix}
$$
上追加热力块，得到总体分块雅可比
$$
J=\begin{bmatrix}
0 & A & 0\\
A^T & -D & 0\\
0 & H_{\dot{m}} & H_T
\end{bmatrix},
$$
其中 $H_{\dot{m}}$ 与 $H_T$ 仅由换热器与等温约束（及源温约束）构成。

2) 源头温度用 $T_1-16=0$ 直接强加；三通之前沿线等温传递用差分约束实现；两台换热器用 $UA$–环境模型闭合能量方程。

3) 迭代更新 $\Delta p,\ \Delta\dot{m},\ \Delta T$，残差趋零即得到耦合后的压力、流量与温度场分布。
