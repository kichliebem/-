

> 拓扑与热交换假设：  
> - 水力网络与之前一致：水源 → 管道 → 阀门 → 三通 → （两支路） → 罐体1、罐体2。  
> - 每条三通后的支路上各有一台换热器 $HX_1$（通向罐体1）、$HX_2$（通向罐体2）。  
> - 两台换热器**仅与“房间”进行换热**；房间与外界**无热交换**。  
> - 源头水温 $T_1=16^\circ\mathrm{C}$；要求随时间求出**房间温度** $T_r$ 的变化（可用隐式时间离散）。

---

## 变量与方程（尺寸对齐）

- 节点压力（6 个）：
  $$
  p=\begin{bmatrix}p_1 & p_2 & p_3 & p_4 & p_5 & p_6\end{bmatrix}^T
  $$
- 支路质量流量（5 条：$b_1,\dots,b_5$）：
  $$
  \dot{m}=\begin{bmatrix}\dot{m}_1 & \dot{m}_2 & \dot{m}_3 & \dot{m}_4 & \dot{m}_5\end{bmatrix}^T
  $$
- 温度变量（“流体节点温度”6 个 + “房间温度”1 个）：
  $$
  \tilde{T}=\begin{bmatrix}T_1 & T_2 & T_3 & T_4 & T_5 & T_6 & T_r\end{bmatrix}^T
  $$

- 残差（方程）组成与数量：
  1) 质量守恒 $R^{(m)}\in\mathbb{R}^{6}$（节点流量平衡）  
  2) 压降/设备方程 $R^{(p)}\in\mathbb{R}^{5}$（支路方程）  
  3) 热力方程 $R^{(h)}\in\mathbb{R}^{7}$：  
     - 3 条“无热损”约束（源→管→阀→三通沿线等温）：$T_2=T_1,\ T_3=T_2,\ T_4=T_3$  
     - 2 条换热器能量方程（$HX_1$：支路 $b_4:4\to5$；$HX_2$：支路 $b_5:4\to6$）  
     - 1 条源头温度约束：$T_1=16$  
     - **1 条房间能量守恒方程**：房间与外界绝热，仅与两台换热器换热

> 总变量数：$6+5+7=18$；总方程数：$6+5+7=18$（配平）。

---

## 水力关联矩阵与阻抗矩阵（与纯水力一致）

- 关联矩阵 $A\in\mathbb{R}^{6\times5}$（行=节点，列=支路；流入 $+1$，流出 $-1$）：
  $$
  A=
  \begin{bmatrix}
  -1 & 0 & 0 & 0 & 0 \\[4pt]
   1 & -1 & 0 & 0 & 0 \\[4pt]
   0 &  1 & -1 & 0 & 0 \\[4pt]
   0 &  0 &  1 & -1 & -1 \\[4pt]
   0 &  0 &  0 &  1 &  0 \\[4pt]
   0 &  0 &  0 &  0 &  1
  \end{bmatrix}
  $$
- 阻抗矩阵 $D=\mathrm{diag}(D_1,\dots,D_5)$（压降–流量导数）：
  $$
  D_b=\frac{\partial\Delta p_b}{\partial \dot{m}_b},\qquad
  \begin{cases}
  \text{管道：} & \displaystyle \frac{\partial\Delta p}{\partial \dot{m}}=2\,k\,\dot{m},\quad
  k=f\,\dfrac{L}{D\,A^2\,2\rho} \\[10pt]
  \text{阀门：} & \displaystyle \frac{\partial\Delta p}{\partial \dot{m}}=\frac{2\,\dot{m}}{C_v^2}
  \end{cases}
  $$
> 本耦合场景下忽略物性随温度的变化（若考虑，可令 $D=D(T)$）。

---

## 热力子系统：换热器仅与房间换热

- 房间等效热容：$C_r$，时间步长：$\Delta t$，隐式离散。
- 定压比热：$c_p$（常数）。
- 换热器 $HX_1$、$HX_2$ 与房间换热系数–面积乘积：$UA_1, UA_2$。

### 热力残差定义

1) 等温（无热损）三条：
   $$
   R^{(h)}_{12}\equiv T_2-T_1=0,\qquad
   R^{(h)}_{23}\equiv T_3-T_2=0,\qquad
   R^{(h)}_{34}\equiv T_4-T_3=0
   $$
2) 两台换热器能量方程（以“流体侧焓变 = 对房间放热”表示），房间温度用 $T_r$ 替代环境温度：
   $$
   \begin{aligned}
   R^{(h)}_{HX_1} &\equiv \dot{m}_4\,c_p\,(T_4-T_5)\;-\;UA_1\,\Big(\tfrac{T_4+T_5}{2}-T_r\Big)=0 \\[6pt]
   R^{(h)}_{HX_2} &\equiv \dot{m}_5\,c_p\,(T_4-T_6)\;-\;UA_2\,\Big(\tfrac{T_4+T_6}{2}-T_r\Big)=0
   \end{aligned}
   $$
3) 源头温度约束：
   $$
   R^{(h)}_{\mathrm{src}}\equiv T_1-16=0
   $$
4) **房间能量守恒（仅与两台换热器换热，绝热于外界）**，采用隐式时间离散：
   $$
   R^{(h)}_{r}\;\equiv\; \frac{C_r}{\Delta t}\,(T_r - T_r^{\,\mathrm{prev}})\;-\;UA_1\Big(\tfrac{T_4+T_5}{2}-T_r\Big)\;-\;UA_2\Big(\tfrac{T_4+T_6}{2}-T_r\Big)\;=\;0
   $$

> 上式物理意义：两台换热器向房间“放热”的总和，使得房间温度在本时间步由 $T_r^{\,\mathrm{prev}}$ 升至 $T_r$，满足能量平衡。

---

## 总体牛顿迭代与分块雅可比

- 变量堆叠：
  $$
  \Delta x=\begin{bmatrix}\Delta p \\[2pt] \Delta\dot{m} \\[2pt] \Delta \tilde{T}\end{bmatrix},
  \qquad
  \tilde{T}=\begin{bmatrix}T_1 & T_2 & T_3 & T_4 & T_5 & T_6 & T_r\end{bmatrix}^T
  $$
- 残差堆叠：
  $$
  R=\begin{bmatrix}R^{(m)} \\[2pt] R^{(p)} \\[2pt] R^{(h)}\end{bmatrix}
  $$
- 牛顿步：
  $$
  J\,\Delta x=-R
  $$

### 分块雅可比结构（忽略 $D$ 的温度依赖）

$$
J=
\begin{bmatrix}
\dfrac{\partial R^{(m)}}{\partial p} & \dfrac{\partial R^{(m)}}{\partial \dot{m}} & \dfrac{\partial R^{(m)}}{\partial \tilde{T}} \\[8pt]
\dfrac{\partial R^{(p)}}{\partial p} & \dfrac{\partial R^{(p)}}{\partial \dot{m}} & \dfrac{\partial R^{(p)}}{\partial \tilde{T}} \\[8pt]
\dfrac{\partial R^{(h)}}{\partial p} & \dfrac{\partial R^{(h)}}{\partial \dot{m}} & \dfrac{\partial R^{(h)}}{\partial \tilde{T}}
\end{bmatrix}
=
\begin{bmatrix}
0 & A & 0 \\[4pt]
A^T & -D & 0 \\[4pt]
0 & H_{\dot{m}} & H_{\tilde{T}}
\end{bmatrix}
$$

- 上两行与纯水力相同。热力–水力一阶耦合在此版本中仅通过 $H_{\dot{m}}$ 的换热器流量项体现。

---

## 热力雅可比子块的显式导数

### 对流量的偏导 $H_{\dot{m}}\in\mathbb{R}^{7\times5}$

只有换热器方程对相应支路流量非零：
$$
\begin{aligned}
\frac{\partial R^{(h)}_{HX_1}}{\partial \dot{m}_4}&=c_p\,(T_4-T_5), &
\frac{\partial R^{(h)}_{HX_1}}{\partial \dot{m}_j}&=0\ (j\neq 4), \\[6pt]
\frac{\partial R^{(h)}_{HX_2}}{\partial \dot{m}_5}&=c_p\,(T_4-T_6), &
\frac{\partial R^{(h)}_{HX_2}}{\partial \dot{m}_j}&=0\ (j\neq 5),
\end{aligned}
$$
其余 5 行（3 条等温、1 条源温、1 条房间能量）对 $\dot{m}$ 的偏导为 0。

---

### 对温度的偏导 $H_{\tilde{T}}\in\mathbb{R}^{7\times7}$

按 $R^{(h)}$ 的 7 条方程顺序（$R^{(h)}_{12},R^{(h)}_{23},R^{(h)}_{34},R^{(h)}_{HX_1},R^{(h)}_{HX_2},R^{(h)}_{\mathrm{src}},R^{(h)}_r$）写出对 $\tilde{T}=[T_1,T_2,T_3,T_4,T_5,T_6,T_r]$ 的偏导矩阵行。

1) 三条等温约束：
$$
\begin{aligned}
\frac{\partial (T_2-T_1)}{\partial \tilde{T}} &= \begin{bmatrix}-1 & 1 & 0 & 0 & 0 & 0 & 0\end{bmatrix} \\[4pt]
\frac{\partial (T_3-T_2)}{\partial \tilde{T}} &= \begin{bmatrix}0 & -1 & 1 & 0 & 0 & 0 & 0\end{bmatrix} \\[4pt]
\frac{\partial (T_4-T_3)}{\partial \tilde{T}} &= \begin{bmatrix}0 & 0 & -1 & 1 & 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

2) 两条换热器能量方程：
$$
\begin{aligned}
\frac{\partial R^{(h)}_{HX_1}}{\partial \tilde{T}} &=
\begin{bmatrix}
0 & 0 & 0 & \ \ \ \dot{m}_4\,c_p-\dfrac{UA_1}{2}\ \ & -\dot{m}_4\,c_p-\dfrac{UA_1}{2} & 0 & \ \ \ UA_1
\end{bmatrix} \\[10pt]
\frac{\partial R^{(h)}_{HX_2}}{\partial \tilde{T}} &=
\begin{bmatrix}
0 & 0 & 0 & \ \ \ \dot{m}_5\,c_p-\dfrac{UA_2}{2}\ \ & 0 & -\dot{m}_5\,c_p-\dfrac{UA_2}{2} & \ \ \ UA_2
\end{bmatrix}
\end{aligned}
$$

3) 源头温度约束：
$$
\frac{\partial (T_1-16)}{\partial \tilde{T}}=\begin{bmatrix}1 & 0 & 0 & 0 & 0 & 0 & 0\end{bmatrix}
$$

4) **房间能量守恒方程**：
$$
R^{(h)}_{r}=\frac{C_r}{\Delta t}\,(T_r - T_r^{\,\mathrm{prev}})-UA_1\Big(\tfrac{T_4+T_5}{2}-T_r\Big)-UA_2\Big(\tfrac{T_4+T_6}{2}-T_r\Big)
$$

对应偏导为：
$$
\frac{\partial R^{(h)}_r}{\partial \tilde{T}}=
\begin{bmatrix}
0 & 0 & 0 & -\dfrac{UA_1}{2}-\dfrac{UA_2}{2} & -\dfrac{UA_1}{2} & -\dfrac{UA_2}{2} & \ \ \ \dfrac{C_r}{\Delta t}+UA_1+UA_2
\end{bmatrix}
$$

---

## 完整雅可比的尺寸与结构

- 变量顺序：
  $$
  \big[p_1,\dots,p_6\ \big|\ \dot{m}_1,\dots,\dot{m}_5\ \big|\ T_1,\dots,T_6,T_r\big]^T
  $$
- 方程顺序：
  $$
  \big[R^{(m)}\ (6)\ ;\ R^{(p)}\ (5)\ ;\ R^{(h)}\ (7)\big]^T
  $$

分块写成：
$$
J=
\begin{bmatrix}
0_{6\times6} & A_{6\times5} & 0_{6\times7} \\[6pt]
A^T_{5\times6} & (-D)_{5\times5} & 0_{5\times7} \\[6pt]
0_{7\times6} & H_{\dot{m}}{}_{7\times5} & H_{\tilde{T}}{}_{7\times7}
\end{bmatrix},
\qquad
J\,\Delta x=-R
$$

---

## 物理与数值要点

- **耦合位置清晰**：  
  - 水力与热力通过换热器方程的 $H_{\dot{m}}$（对 $\dot{m}_4,\dot{m}_5$ 的导数）产生耦合；  
  - 两台换热器与**房间温度 $T_r$** 强耦合，体现于 $H_{\tilde{T}}$ 的 $UA_1,UA_2$ 项；  
  - 房间方程在时间离散后为**代数方程**（隐式方法），兼具良好稳定性。
- **稳态特例**：若关注稳态房间温度，可令 $\dfrac{C_r}{\Delta t}\to 0$，则 $R^{(h)}_r$ 退化为
  $$
  -UA_1\Big(\tfrac{T_4+T_5}{2}-T_r\Big)-UA_2\Big(\tfrac{T_4+T_6}{2}-T_r\Big)=0,
  $$
  与两台换热器方程共同给出稳态 $T_r$。
- **求解建议**：  
  1) 先解纯水力得到 $\{\dot{m}\}$ 初值，再开启热力–房间耦合；  
  2) $UA$ 很大或流量极小会增大刚性，必要时对温度方程做松弛或线搜索；  
  3) 若需更精细换热模型（$\varepsilon$–NTU 或 LMTD），将 $R^{(h)}_{HX_i}$ 与其偏导替换即可，$R^{(h)}_r$ 仅需相应替换“传至房间的热量”项。

---

## 小结

- 在原水力雅可比
  $$
  \begin{bmatrix}0 & A\\ A^T & -D\end{bmatrix}
  $$
  基础上，加入“流体温度 + 房间温度”热力块，得到：
  $$
  J=\begin{bmatrix}
  0 & A & 0\\
  A^T & -D & 0\\
  0 & H_{\dot{m}} & H_{\tilde{T}}
  \end{bmatrix},
  $$
  其中 $H_{\dot{m}}$、$H_{\tilde{T}}$ 的非零项仅由换热器与房间能量方程构成。通过隐式时间推进即可得到房间温度 $T_r$ 的动态响应，以及全网压力、流量与温度的耦合解。
